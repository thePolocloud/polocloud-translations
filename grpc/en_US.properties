grpc.start.alreadyRunning=gRPC server is already running on {address}.
grpc.start.starting=Starting gRPC server on {address}...
grpc.start.tls.enabled=TLS enabled (clientAuth={clientAuth})
grpc.start.success=gRPC server started successfully and is reporting {servingStatus}.

grpc.shutdown.notRunning=gRPC server is not running, nothing to close.
grpc.shutdown.starting=Shutting down gRPC server...
grpc.shutdown.force=Forcing immediate shutdown of gRPC server.
grpc.shutdown.timeout=Server did not terminate in {timeout} seconds, forcing shutdown.
grpc.shutdown.success=gRPC server terminated gracefully.
grpc.shutdown.interrupted=Shutdown interrupted, forcing immediate shutdown.

grpc.bind_failed=Failed to bind gRPC server to '{address}'. The port may already be in use.